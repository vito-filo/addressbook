# nginx.conf — Serves the React SPA; all paths fall back to index.html for client-side routing.
server {
  listen 80;

  location / {
    root /usr/share/nginx/html;
    index index.html;
    try_files $uri $uri/ /index.html;
  }
}
